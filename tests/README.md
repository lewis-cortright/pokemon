Testing

This project uses Vitest and targets 100% coverage for application-owned business logic, including formatting, mapping, filtering, and API response shaping.

Run tests:

pnpm test

Run coverage:

pnpm test:coverage
